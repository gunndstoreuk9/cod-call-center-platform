'use client'
import { useEffect,useState } from 'react'
import { api,money } from '../../../lib/api'
import { useI18n } from '../../../lib/i18n'
import Modal from '../../../components/Modal'
import StatusBadge from '../../../components/StatusBadge'
const empty={username:'',password:'',display_name:'',phone:'',email:'',commission_default:'5',product_ids:[]}
export default function AgentsPage(){const{t}=useI18n();const[agents,setAgents]=useState([]),[products,setProducts]=useState([]),[open,setOpen]=useState(false),[form,setForm]=useState(empty),[error,setError]=useState(''),[saving,setSaving]=useState(false);const load=()=>Promise.all([api('/agents'),api('/products?status=ACTIVE')]).then(([a,p])=>{setAgents(a);setProducts(p)}).catch(e=>setError(e.message));useEffect(()=>{load()},[]);const toggleProduct=id=>setForm(f=>({...f,product_ids:f.product_ids.includes(id)?f.product_ids.filter(x=>x!==id):[...f.product_ids,id]}));const submit=async e=>{e.preventDefault();setSaving(true);setError('');try{await api('/agents',{method:'POST',body:{...form,commission_default:Number(form.commission_default),role:'AGENT'}});setOpen(false);setForm(empty);await load()}catch(err){setError(err.message)}finally{setSaving(false)}};const toggle=async a=>{try{await api(`/agents/${a.id}`,{method:'PATCH',body:{is_active:!a.is_active}});await load()}catch(e){setError(e.message)}};return <><div className="page-head"><div><h1>{t('Agents')}</h1><p>{t('Create agents, assign products, control commission and access.')}</p></div><button className="btn" onClick={()=>setOpen(true)}>{t('+ Add Agent')}</button></div>{error&&<div className="error">{error}</div>}<div className="panel"><div className="table-wrap"><table className="table"><thead><tr><th>{t('Agent')}</th><th>{t('Username')}</th><th>{t('Products')}</th><th>{t('Default Commission')}</th><th>{t('Unpaid Balance')}</th><th>{t('Status')}</th><th>{t('Action')}</th></tr></thead><tbody>{agents.map(a=><tr key={a.id}><td><strong>{a.display_name}</strong></td><td>@{a.username}</td><td>{a.product_ids.length}</td><td>{money(a.commission_default)}</td><td><strong>{money(a.current_balance)}</strong></td><td><StatusBadge value={a.is_active?'ACTIVE':'INACTIVE'}/></td><td><button className="btn small secondary" onClick={()=>toggle(a)}>{a.is_active?t('Disable'):t('Enable')}</button></td></tr>)}</tbody></table></div></div><Modal open={open} title="Add Agent" onClose={()=>setOpen(false)} wide><form onSubmit={submit}><div className="form-grid"><div className="field"><label>{t('Display Name')}</label><input required value={form.display_name} onChange={e=>setForm({...form,display_name:e.target.value})}/></div><div className="field"><label>{t('Username')}</label><input required value={form.username} onChange={e=>setForm({...form,username:e.target.value})}/></div><div className="field"><label>{t('Password')}</label><input type="password" minLength="8" required value={form.password} onChange={e=>setForm({...form,password:e.target.value})}/></div><div className="field"><label>{t('Default Commission')}</label><input type="number" min="0" step="0.01" value={form.commission_default} onChange={e=>setForm({...form,commission_default:e.target.value})}/></div><div className="field"><label>{t('Phone')}</label><input value={form.phone} onChange={e=>setForm({...form,phone:e.target.value})}/></div><div className="field"><label>Email</label><input type="email" value={form.email} onChange={e=>setForm({...form,email:e.target.value})}/></div><div className="field full"><label>{t('Allowed Products')}</label><div className="check-grid">{products.map(p=><label className="check" key={p.id}><input type="checkbox" checked={form.product_ids.includes(p.id)} onChange={()=>toggleProduct(p.id)}/><span>{p.name}<br/><small>{p.sku}</small></span></label>)}</div></div></div><div className="form-actions"><button type="button" className="btn secondary" onClick={()=>setOpen(false)}>{t('Cancel')}</button><button className="btn" disabled={saving}>{saving?t('Creating...'):t('Create Agent')}</button></div></form></Modal></>}
