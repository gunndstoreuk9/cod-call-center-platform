'use client'
import { useEffect,useState } from 'react'
import { api,money } from '../../../lib/api'
import { useI18n } from '../../../lib/i18n'
import Modal from '../../../components/Modal'
import StatusBadge from '../../../components/StatusBadge'
const empty={store_id:'',name:'',sku:'',image_url:'',selling_price:'',currency:'MAD',default_qty:1,delivery_product_ref:'',commission_per_confirmation:'5',status:'ACTIVE'}
export default function ProductsPage(){const{t}=useI18n();const[products,setProducts]=useState([]),[stores,setStores]=useState([]),[open,setOpen]=useState(false),[editing,setEditing]=useState(null),[form,setForm]=useState(empty),[error,setError]=useState(''),[saving,setSaving]=useState(false);const load=()=>Promise.all([api('/products'),api('/stores')]).then(([p,s])=>{setProducts(p);setStores(s);if(!form.store_id&&s[0])setForm(f=>({...f,store_id:s[0].id}))}).catch(e=>setError(e.message));useEffect(()=>{load()},[]);const newProduct=()=>{setEditing(null);setForm({...empty,store_id:stores[0]?.id||''});setOpen(true)};const editProduct=p=>{setEditing(p);setForm({store_id:p.store_id,name:p.name,sku:p.sku,image_url:p.image_url||'',selling_price:p.selling_price,currency:p.currency,default_qty:p.default_qty,delivery_product_ref:p.delivery_product_ref||'',commission_per_confirmation:p.commission_per_confirmation??'',status:p.status});setOpen(true)};const submit=async e=>{e.preventDefault();setSaving(true);setError('');try{const body={...form,selling_price:Number(form.selling_price),default_qty:Number(form.default_qty),commission_per_confirmation:form.commission_per_confirmation===''?null:Number(form.commission_per_confirmation),delivery_product_ref:form.delivery_product_ref||null,image_url:form.image_url||null};if(editing)await api(`/products/${editing.id}`,{method:'PATCH',body});else await api('/products',{method:'POST',body:{...body,offers:[]}});setOpen(false);setEditing(null);await load()}catch(err){setError(err.message)}finally{setSaving(false)}};const toggle=async p=>{try{await api(`/products/${p.id}`,{method:'PATCH',body:{status:p.status==='ACTIVE'?'INACTIVE':'ACTIVE'}});await load()}catch(e){setError(e.message)}};return <><div className="page-head"><div><h1>{t('Products')}</h1><p>{t('Add and edit products from Admin — including Digylog delivery designation.')}</p></div><button className="btn" onClick={newProduct}>{t('+ Add Product')}</button></div>{error&&<div className="error">{error}</div>}<div className="panel"><div className="table-wrap"><table className="table"><thead><tr><th>{t('Product')}</th><th>{t('SKU')}</th><th>{t('Price')}</th><th>{t('Delivery Ref')}</th><th>{t('Commission')}</th><th>{t('Offers')}</th><th>{t('Status')}</th><th>{t('Action')}</th></tr></thead><tbody>{products.map(p=><tr key={p.id}><td><strong>{p.name}</strong></td><td>{p.sku}</td><td>{money(p.selling_price,p.currency)}</td><td>{p.delivery_product_ref||<small>{t('Uses product name')}</small>}</td><td>{p.commission_per_confirmation==null?t('Agent default'):money(p.commission_per_confirmation,p.currency)}</td><td>{p.offers?.length||0}</td><td><StatusBadge value={p.status}/></td><td><div style={{display:'flex',gap:6,flexWrap:'wrap'}}><button className="btn small" onClick={()=>editProduct(p)}>{t('Edit')}</button><button className="btn small secondary" onClick={()=>toggle(p)}>{p.status==='ACTIVE'?t('Disable'):t('Enable')}</button></div></td></tr>)}</tbody></table></div></div><Modal open={open} title={editing?'Edit Product':'Add Product'} onClose={()=>setOpen(false)}><form onSubmit={submit}><div className="form-grid"><div className="field full"><label>{t('Store')}</label><select required value={form.store_id} onChange={e=>setForm({...form,store_id:e.target.value})}>{stores.map(s=><option key={s.id} value={s.id}>{s.name}</option>)}</select></div><div className="field"><label>{t('Product Name')}</label><input required value={form.name} onChange={e=>setForm({...form,name:e.target.value})}/></div><div className="field"><label>{t('SKU')}</label><input required value={form.sku} onChange={e=>setForm({...form,sku:e.target.value})}/></div><div className="field"><label>{t('Selling Price')}</label><input type="number" step="0.01" min="0" required value={form.selling_price} onChange={e=>setForm({...form,selling_price:e.target.value})}/></div><div className="field"><label>{t('Commission / Confirmation')}</label><input type="number" step="0.01" min="0" value={form.commission_per_confirmation} onChange={e=>setForm({...form,commission_per_confirmation:e.target.value})}/></div><div className="field"><label>{t('Currency')}</label><input value={form.currency} onChange={e=>setForm({...form,currency:e.target.value.toUpperCase()})}/></div><div className="field"><label>{t('Default Qty')}</label><input type="number" min="1" value={form.default_qty} onChange={e=>setForm({...form,default_qty:e.target.value})}/></div><div className="field full"><label>{t('Delivery Product Ref / Digylog Designation')}</label><input placeholder={t('Leave empty to send the product name')} value={form.delivery_product_ref} onChange={e=>setForm({...form,delivery_product_ref:e.target.value})}/></div><div className="field full"><label>{t('Image URL (optional)')}</label><input value={form.image_url} onChange={e=>setForm({...form,image_url:e.target.value})}/></div></div><div className="form-actions"><button type="button" className="btn secondary" onClick={()=>setOpen(false)}>{t('Cancel')}</button><button className="btn" disabled={saving}>{saving?t('Saving...'):editing?t('Save Changes'):t('Save Product')}</button></div></form></Modal></>}
